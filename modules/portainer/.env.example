# =============================================================================
# Portainer environment configuration
# =============================================================================
# No mandatory variables for basic setup.
# Portainer admin user is created on first web login.
#
# Optional: uncomment to set admin password non-interactively
# (use: htpasswd -nbB admin 'your_password' | cut -d: -f2)
# PORTAINER_ADMIN_PASSWORD_HASH=
