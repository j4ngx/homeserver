# =============================================================================
# Nginx Proxy Manager — Custom Nginx snippets (advanced)
# =============================================================================
# Place .conf files in this directory to inject custom Nginx directives into
# individual proxy host configurations via the NPM UI.
#
# Usage in NPM UI → Proxy Host → Advanced → Custom Nginx Configuration:
#   include /data/nginx/custom/<file>.conf;
#
# Files here are NOT automatically loaded — they must be referenced from NPM.
# =============================================================================
